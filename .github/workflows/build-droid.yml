name: Build Agent Image (droid)

"on":
  push:
    tags:
      - "*"
  workflow_dispatch: {}

jobs:
  dispatch:
    uses: ./.github/workflows/build-tool.yml
    with:
      tool: droid
    secrets: inherit
