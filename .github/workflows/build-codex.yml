name: Build Agent Image (codex)

"on":
  push:
    tags:
      - "*"
  workflow_dispatch: {}

jobs:
  dispatch:
    uses: ./.github/workflows/build-tool.yml
    with:
      tool: codex
    secrets: inherit
