name: Build Agent Image (cline)

"on":
  push:
    tags:
      - "*"
  workflow_dispatch: {}

jobs:
  dispatch:
    uses: ./.github/workflows/build-tool.yml
    with:
      tool: cline
    secrets: inherit
